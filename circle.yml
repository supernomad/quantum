machine:
  timezone:
    America/New_York
  environment:
    PATH: "/usr/local/go/bin:$PATH"
dependencies:
  override:
    - sudo bash -c "export PATH=$PATH && cd $(pwd) && make dependancies"

compile:
  override:
    - sudo bash -c "export PATH=$PATH && cd $(pwd) && make ci"

test:
  override:
    - sudo bash -c "export PATH=$PATH && cd $(pwd) && make benchmark"
