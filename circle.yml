machine:
  timezone:
    America/New_York
  environment:
    PATH: "/usr/local/go/bin:$PATH"
dependencies:
  override:
    - sudo -i -E bash -c "cd $(pwd) && make dependancies"

compile:
  override:
    - sudo -i -E bash -c "cd $(pwd) && make ci"

test:
  override:
    - sudo -i -E bash -c "cd $(pwd) && make benchmark"
