machine:
  timezone:
    America/New_York
  environment:
    PATH: "/usr/local/go/bin:$PATH"
dependencies:
  override:
    - sudo bash -c "export PATH=$PATH && export GOPATH=$GOPATH && export HOME=$HOME && cd $(pwd) && make dependancies"

compile:
  override:
    - sudo bash -c "export PATH=$PATH && export GOPATH=$GOPATH && export HOME=$HOME && cd $(pwd) && make ci"

test:
  override:
    - sudo bash -c "export PATH=$PATH && export GOPATH=$GOPATH && export HOME=$HOME && cd $(pwd) && make benchmark"
